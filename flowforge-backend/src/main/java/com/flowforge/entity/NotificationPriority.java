package com.flowforge.entity;

/**
 * Notification Priority Enum.
 * 
 * PURPOSE:
 * Defines urgency priority levels (HIGH, MEDIUM, LOW) for filtering and styling
 * notifications.
 * 
 * WHERE USED:
 * Used in Notification entity to render accent borders and priority indicators.
 */
public enum NotificationPriority {
    HIGH,
    MEDIUM,
    LOW
}
